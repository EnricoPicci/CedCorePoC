/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IbanPipe } from './iban.pipe';

describe('Pipe: Iban', () => {
  it('create an instance', () => {
    let pipe = new IbanPipe();
    expect(pipe).toBeTruthy();
  });
});
